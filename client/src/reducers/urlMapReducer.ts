import type { URLMapStateType } from "../contexts/urlMapContext";
import type Action from "../model/Action";


export default function urlMapReducer(urlMap: URLMapStateType, action: Action){
    
    return urlMap
}