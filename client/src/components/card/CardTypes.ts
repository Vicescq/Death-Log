export type CardModalStateGame = {
    name: string;
};

export type CardModalStateProfile = {
    name: string;
};

export type CardModalStateSubject = {
    name: string;
    reoccurring: boolean;
    composite: boolean;
};
